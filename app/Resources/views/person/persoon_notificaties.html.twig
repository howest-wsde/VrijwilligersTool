{% trans_default_domain "views" %}
{% if notifications|length>0 %}

    <section class="part">
        <div class="container tabblad spacing">
            <ul class="nav nav-tabs">
                <li class="blauw active"><a href="#notifications" id="notificationsTab" data-toggle="tab">{{  notifications|length }} {{ "person.dash.notifications.notifications"|trans }}</a></li>
            </ul>

            <div class="tab-content clearfix blok special">
                <div class="tab-pane active" id="notifications">
                    <div class="blokken">
                        {% for notification in notifications %}
                            <div class="blok cta">
                                <h4>{{ notification.text }}</h4>
                                <div class="cta">
                                <a href="{{ notification.actionLink }}" class="cta blauw">{{ "person.dash.notifications.see"|trans }}</a>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            </div>

        </div>

        <script>
            var intervalNotifications = setInterval(function() {
                if (typeof $ == 'undefined') return;
                clearInterval(intervalNotifications);
                $("#notificationsTab").css("cursor", "pointer");
                $("#notificationsTab").unbind().on("click", function(){
                    $("#notifications").toggle();
                })
            }, 10);
        </script>
    </section>

{% endif %}