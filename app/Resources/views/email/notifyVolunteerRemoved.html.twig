{% trans_default_domain "mail" %}
{% extends "emailbase.html.twig" %}

{% block body %}
	<h1>{{ "notVolRem.header" |trans }}</h1>

	<p>{{ "notVolRem.firstp" |trans }} {{ candidate.firstname }},</p>

	<p>
    {{ "notVolRem.secondp.fpart" |trans }} <a href="{{ url("vacancy_by_urlid", { "urlid": vacancy.urlId }) }}">{{ vacancy.title }}</a>.<br />
        {{ "notVolRem.secondp.spart" | trans }}
	</p>

{% endblock %}