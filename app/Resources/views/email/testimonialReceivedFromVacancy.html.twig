{% trans_default_domain "mail" %}
{% extends "emailbase.txt.twig" %}

{% block body %}
    <h1>{{ "testimonialFromVacancy.header" | trans }}</h1>

    <p>{{ "testimonialFromVacancy.firstp" |trans }} {{ user.firstname }} {{ user.lastname }},</p>

    <p>
        {{ "testimonialFromVacancy.secondp.fpart" |trans }}{{ org.name }} {{ "testimonialFromVacancy.secondp.spart" | trans }} <a href="{{ url('vacancy_by_urlid', {'urlid': vacancy.urlid }) }}">"{{ vacancy.title }}".</a>
    </p>
    <p>
        <a href="{{ url("person_username", {'username': user.username }) }}">"{{ "testimonialFromVacancy.secondp.tpart" |trans }}".</a>
    </p>

{% endblock %}

